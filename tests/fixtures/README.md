# Fixtures

Store captured shell prototype outputs and deterministic input samples here.

- `render-sample.txt`: deterministic render input shared by snapshot tests
- `shell-welcome-wide.txt` / `shell-welcome-narrow.txt`: ANSI-stripped shell welcome captures from the reference script
- `shell-services-linux.txt`: ANSI-stripped Linux `sss` capture from the reference script
